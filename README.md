# eMerge Media. Landing Page.

A one-page read prepared for the principal at booth 463, eMerge Americas 2026. Editorial creative-agency frame: paper stock ground, serif display, single terracotta accent. Drops the 60/30/10 frame on where AI actually fits in a small creative shop's stack, matches KPMG UK as the nearest posture case, and closes on a 30 minute calendar link with Matt Creamer.

Intended URL path: /for/emerge-media

## Files

- `index.html`. Single-file static page.
- `styles.css`. Company-native CSS, brand-pack tokens only.
- `logo.svg`. Wordmark (no logo asset was available for the exhibitor; SVG wordmark built from brand-pack typography).

## Notes for Matt at the booth

- The brand pack is L confidence. Confirm identity in the first question (agency vs. conference-arm). If the principal is part of the eMerge Americas team, do not hand over this page.
- No pricing anywhere on the page. Scope language only.
- NLP Logix is intentionally not mentioned. If the principal asks where Eduba sends work below the orchestration layer, say the partnership line verbally.
- Page loads fonts from Google Fonts (Fraunces, Inter, JetBrains Mono) to match the brand-pack typography system. If the principal's real brand is different, replace before any public hosting.
